(function() {
    // This minified version replace the public methods to noop operations.
    var r = this,
        or = r.argsContract,
        m = 'argsContract';
    r[m] = function(a, b) {
    };
    r[m].noConflict = function() {
        if (_.isUndefined(or)) {
            r[m] = undefined;
            try {
                delete r[m];
            } catch (e) {
            }
        }
        r[m] = or;
    };
}).call(this);
